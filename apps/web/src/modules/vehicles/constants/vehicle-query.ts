import type { ListVehiclesParams } from '../types/vehicle.types';

/** Shared RTK Query cache key for the customer vehicle list */
export const DEFAULT_VEHICLE_LIST_QUERY: ListVehiclesParams = { limit: 100 };
